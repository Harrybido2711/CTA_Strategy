# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Local tooling — keep out of the repo
.claude/
CLAUDE.md
skills/

# Personal chat records — keep out of the repo
chat_records/

# Recording transcripts — source material, not notes
docs/recording_script/

# LaTeX scratch — wrappers, .aux, .log, .toc. Regenerate with docs/build.sh.
# The PDFs themselves are committed: read_only_chapters/ and all_chapters.pdf
# are what a reader without a TeX install opens.
docs/_build/
docs/chapters/*/_build/

# Agent entry points — local only, like .claude/
AGENTS.md
