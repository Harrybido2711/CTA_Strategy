# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Local tooling — keep out of the repo
.claude/
CLAUDE.md
skills/

# Personal chat records — keep out of the repo
chat_records/

# Recording transcripts — source material, not notes
docs/recording_script/

# Typeset output and LaTeX scratch — regenerate with docs/tex/build.sh
docs/_pdf/
docs/tex/_build/
